# sops configuration
# Copy to .sops.yaml and replace with your age public key.
#
# Generate a key:  age-keygen -o ~/.config/sops/age/keys.txt
# The public key is printed to stdout — paste it below.
creation_rules:
  - path_regex: secrets/.*\.yaml$
    age: >-
      age1your-public-key-here
